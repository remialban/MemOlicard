{% extends 'dashboard/base.html.twig' %}

{% block title %}{{ post.title }} | Blog{% endblock %}

{% block content %}
<div class="container">
    <h1>{{ post.title }}</h1>
    <hr/>
    {{ post.content|raw }}
</div>
{% endblock %}
